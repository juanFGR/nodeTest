var config = {
	twitter:{
		key:'Consumer Key (API Key)',
		secret:'Consumer Secret (API Secret)'
	},
	facebook:{
		key:'Consumer Key (API Key)',
		secret:'Consumer Secret (API Secret)'
	}

	
}
module.exports = config;
